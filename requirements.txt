# 핵심 (필수)
opencv-python-headless>=4.8,<4.12
moviepy>=2.1,<3
imageio-ffmpeg>=0.5
Pillow>=10
numpy>=1.26,<2
scipy>=1.11

# 얼굴 감지 정확도 (강력 권장)
# 모델 파일이 패키지에 포함돼 있어 별도 다운로드가 필요 없습니다.
# 없으면 OpenCV Haar cascade 로 자동 대체되지만 감지율이 크게 떨어집니다.
mediapipe>=0.10.9,<0.11

# 사람 검출 기반 감지 (교실 사진에 강력 권장)
# 라이선스: AGPL-3.0. 외부 배포 시 확인 필요.
# 설치 후 `python scripts/download_models.py` 로 가중치를 받으세요.
ultralytics>=8.0,<9

# 자동 자막 생성 (--ai-captions, 선택)
# ANTHROPIC_API_KEY 환경변수가 필요합니다. 없으면 기본 템플릿으로 동작합니다.
anthropic>=1.0,<2
