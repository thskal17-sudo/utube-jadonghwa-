# 핵심 (필수)
# numpy 2.x / OpenCV 5.x 에서도 동작을 확인했습니다(테스트 전 구간 통과).
opencv-python-headless>=4.8
moviepy>=2.1,<3
imageio-ffmpeg>=0.5
Pillow>=10
numpy>=1.26
scipy>=1.11

# 얼굴 감지 정확도 (강력 권장)
# 없으면 OpenCV Haar cascade 로 자동 대체되지만 감지율이 크게 떨어집니다.
#
# 0.10.30 부터 레거시 solutions API 가 제거되고 모델이 패키지에서 빠졌습니다.
# 그 버전 이상에서는 두 가지가 더 필요합니다(shorts_pipeline/mp_compat.py 가 자동 판별).
#   1) python scripts/download_models.py  ← Tasks API 모델 파일
#   2) sudo apt install libgles2 libegl1  ← 리눅스 시스템 라이브러리
# 0.10.35 와 1.0.1 에서 동작을 확인했습니다.
mediapipe>=0.10.9

# 사람 검출 기반 감지 (교실 사진에 강력 권장)
# 라이선스: AGPL-3.0. 외부 배포 시 확인 필요.
# 설치 후 `python scripts/download_models.py` 로 가중치를 받으세요.
ultralytics>=8.0,<9
